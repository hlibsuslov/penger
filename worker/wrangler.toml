name = "penger-ai-tutor"
main = "src/index.js"
compatibility_date = "2024-09-01"

[vars]
ALLOWED_ORIGIN = "https://penger.io"
MAX_INPUT_TOKENS = "800"
MAX_OUTPUT_TOKENS = "500"
RATE_LIMIT_RPM = "20"

# Secret binding — set via: wrangler secret put OPENAI_API_KEY
# Never store the actual key in this file.
