<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
  <%- include('../partials/head') %>
  <% if (typeof extraCss !== 'undefined') { extraCss.forEach(function(css) { %>
  <link rel="stylesheet" href="/<%= css %>">
  <% }); } %>
  <title><%= t.order.checkoutTitle || 'Checkout — PENGER' %></title>
  <style>
    /* ===== CHECKOUT PAGE LAYOUT ===== */
    .checkout-page { padding: 48px 0 80px; }
    .checkout-page > .container { max-width: 1400px; }

    .checkout-grid {
      display: grid;
      grid-template-columns: 1fr 380px;
      gap: 48px;
      align-items: start;
    }
    @media (min-width: 1200px) {
      .checkout-grid {
        grid-template-columns: 1fr 400px;
        gap: 80px;
      }
    }
    @media (min-width: 1600px) {
      .checkout-grid {
        grid-template-columns: 1fr 420px;
        gap: 120px;
      }
    }

    /* Left column: checkout forms */
    .checkout-forms { max-width: 520px; }

    /* Back link */
    .order-back {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-family: var(--font-sans);
      font-size: 13px;
      font-weight: 600;
      letter-spacing: 0.01em;
      color: var(--text-primary);
      text-decoration: none;
      margin-bottom: 28px;
      padding: 8px 14px 8px 10px;
      border: 1px solid var(--border-default);
      border-radius: 8px;
      background: var(--bg-surface);
      transition: border-color 0.2s ease, color 0.2s ease, background 0.2s ease;
    }
    .order-back:hover {
      border-color: var(--text-accent);
      color: var(--text-accent);
    }
    .order-back svg { width: 16px; height: 16px; }

    /* ===== CHECKOUT STEPS ===== */
    .checkout-step {
      background: var(--bg-surface);
      border: 1px solid var(--border-default);
      border-radius: 10px;
      margin-bottom: 16px;
      overflow: hidden;
      transition: opacity 0.3s ease, transform 0.3s ease, box-shadow 0.3s ease;
    }
    .checkout-step.active {
      box-shadow: 0 2px 12px rgba(0,0,0,0.06);
    }
    .checkout-step.disabled { opacity: 0.4; pointer-events: none; }
    .step-body {
      padding: 0 20px 20px;
      transition: max-height 0.35s ease, opacity 0.25s ease;
      overflow: hidden;
    }
    .checkout-step.active .step-body { opacity: 1; }
    .checkout-step:not(.active) .step-body { opacity: 0; max-height: 0 !important; padding: 0 20px 0; }
    .checkout-step.step-entering { animation: stepSlideIn 0.3s ease forwards; }
    @keyframes stepSlideIn {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .step-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 14px 20px;
      cursor: default;
    }
    .step-header-left {
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .step-number {
      width: 26px; height: 26px;
      display: flex; align-items: center; justify-content: center;
      border-radius: 50%;
      font-family: var(--font-mono);
      font-size: 11px;
      font-weight: 600;
      background: var(--bg-main);
      border: 1.5px solid var(--border-default);
      color: var(--text-dim);
      flex-shrink: 0;
      transition: all 0.2s ease;
    }
    .checkout-step.active .step-number,
    .checkout-step.completed .step-number {
      border-color: var(--text-accent);
      color: var(--text-accent);
    }
    .checkout-step.completed .step-number {
      background: var(--text-accent);
      color: var(--bg-main);
    }
    .step-title {
      font-family: var(--font-mono);
      font-size: 11px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      color: var(--text-dim);
      transition: color 0.2s ease;
    }
    .checkout-step.active .step-title,
    .checkout-step.completed .step-title {
      color: var(--text-accent);
    }
    .step-edit-btn {
      font-family: var(--font-mono);
      font-size: 10px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      color: var(--text-accent);
      background: none;
      border: 1px solid var(--border-default);
      border-radius: 4px;
      padding: 4px 10px;
      cursor: pointer;
      display: none;
      transition: background 0.15s ease, color 0.15s ease, border-color 0.15s ease;
    }
    .step-edit-btn:hover {
      border-color: var(--text-accent);
      background: var(--text-accent);
      color: var(--bg-main);
    }
    .checkout-step.completed .step-edit-btn { display: inline-flex; }
    .step-summary {
      display: none;
      padding: 0 20px 14px;
      font-family: var(--font-sans);
      font-size: 13px;
      color: var(--text-primary);
      line-height: 1.5;
    }
    .checkout-step.completed .step-summary { display: block; }
    .step-summary-line {
      display: flex;
      gap: 10px;
      align-items: baseline;
      padding: 2px 0;
    }
    .step-summary-label {
      color: var(--text-dim);
      font-family: var(--font-sans);
      font-size: 12px;
      font-weight: 500;
      min-width: 120px;
      flex-shrink: 0;
    }

    /* Form fields */
    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2px;
      margin-bottom: 0;
    }
    .form-row.full { grid-template-columns: 1fr; }
    #deliveryForm .form-row { margin-bottom: 0; }
    #deliveryForm .form-row .form-group { gap: 0; }
    .form-group { display: flex; flex-direction: column; gap: 0; position: relative; }
    .form-label {
      font-family: var(--font-mono);
      font-size: 10px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      color: var(--text-dim);
    }
    .form-input {
      font-family: var(--font-sans);
      font-size: 16px;
      color: var(--text-primary);
      background: var(--bg-main);
      border: 1px solid var(--border-default);
      border-radius: 6px;
      padding: 8px 12px;
      outline: none;
      transition: border-color 0.15s ease;
    }
    .form-input:focus { border-color: var(--text-accent); }
    .form-input.error { border-color: #e74c3c; }
    .form-input::placeholder { color: var(--text-dim); opacity: 0.5; }
    .zip-suggestions {
      position: absolute;
      top: 100%; left: 0; right: 0; z-index: 10;
      background: var(--bg-main);
      border: 1px solid var(--border-default);
      border-top: none;
      border-radius: 0 0 6px 6px;
      max-height: 160px;
      overflow-y: auto;
      display: none;
    }
    .zip-suggestions.open { display: block; }
    .zip-suggestion {
      padding: 8px 12px;
      font-family: var(--font-sans);
      font-size: 13px;
      color: var(--text-primary);
      cursor: pointer;
      transition: background 0.1s ease;
    }
    .zip-suggestion:hover { background: var(--bg-surface); }
    .zip-suggestion .zip-code {
      font-family: var(--font-mono);
      font-size: 12px;
      color: var(--text-dim);
      margin-left: 6px;
    }
    select.form-input {
      appearance: none;
      background-image: url("data:image/svg+xml,%3Csvg width='10' height='6' viewBox='0 0 10 6' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 1L5 5L9 1' stroke='%23666' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'/%3E%3C/svg%3E");
      background-repeat: no-repeat;
      background-position: right 12px center;
      padding-right: 32px;
      padding-top: 11px;
      padding-bottom: 11px;
    }
    .form-privacy {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 10px 12px;
      background: var(--bg-main);
      border: 1px solid var(--border-default);
      border-radius: 6px;
      margin-bottom: 12px;
    }
    .form-privacy svg {
      width: 13px; height: 13px;
      color: var(--text-dim);
      flex-shrink: 0;
    }
    .form-privacy span {
      font-family: var(--font-sans);
      font-size: 11px;
      color: var(--text-dim);
      line-height: 1.4;
    }
    .step-continue-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      width: 100%;
      padding: 13px 20px;
      margin-top: 8px;
      font-family: var(--font-mono);
      font-size: 13px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      color: var(--bg-main);
      background: var(--text-accent);
      border: 1px solid var(--text-accent);
      border-radius: 8px;
      cursor: pointer;
      transition: background 0.2s ease, box-shadow 0.2s ease;
      min-height: 44px;
    }
    .step-continue-btn:hover { background: var(--text-accent-hover); }

    /* Payment methods */
    .pay-methods { display: flex; flex-direction: column; gap: 8px; margin-bottom: 16px; }
    .pay-method {
      display: flex; align-items: center; gap: 12px;
      padding: 14px 16px; background: var(--bg-main);
      border: 1.5px solid var(--border-default); border-radius: 8px;
      cursor: pointer; transition: border-color 0.15s ease;
    }
    .pay-method:hover { border-color: var(--border-hover); }
    .pay-method.active { border-color: var(--text-accent); }
    .pay-method input { display: none; }
    .pay-radio {
      width: 18px; height: 18px;
      border: 2px solid var(--border-default); border-radius: 50%;
      flex-shrink: 0; display: flex; align-items: center; justify-content: center;
      transition: border-color 0.15s ease;
    }
    .pay-method.active .pay-radio { border-color: var(--text-accent); }
    .pay-radio-dot {
      width: 8px; height: 8px; border-radius: 50%;
      background: var(--text-accent); display: none;
    }
    .pay-method.active .pay-radio-dot { display: block; }
    .pay-method-info { flex: 1; display: flex; flex-direction: column; gap: 2px; }
    .pay-method-name { font-family: var(--font-sans); font-size: 14px; font-weight: 600; color: var(--text-accent); }
    .pay-method-desc { font-family: var(--font-sans); font-size: 12px; color: var(--text-dim); display: block; }
    .pay-method-icon { width: 20px; height: 20px; flex-shrink: 0; color: var(--text-dim); }
    .pay-method.active .pay-method-icon { color: var(--text-accent); }
    .pay-method-icons { display: flex; gap: 4px; align-items: center; }
    .pay-method-icons span {
      font-family: var(--font-mono); font-size: 9px; font-weight: 700;
      text-transform: uppercase; letter-spacing: 0.04em;
      padding: 3px 6px; border: 1px solid var(--border-default);
      border-radius: 3px; color: var(--text-dim); background: var(--bg-surface);
    }
    .card-fields { margin-top: 12px; }
    .card-fields .form-row { margin-bottom: 4px; }
    .card-fields.hidden { display: none; }

    /* Solana Checkout */
    .solana-checkout { margin-top: 16px; padding: 20px; border-radius: 6px; background: var(--bg-surface); border: 1px solid var(--border-default); }
    .solana-checkout.hidden { display: none; }
    .solana-asset-select { display: flex; gap: 8px; margin-bottom: 16px; }
    .solana-asset-btn {
      flex: 1; padding: 10px 12px; border-radius: 6px; border: 1px solid var(--border-default);
      background: var(--bg-main); cursor: pointer; text-align: center; transition: border-color 0.2s ease, background 0.2s ease;
      font-family: var(--font-mono); font-size: 12px; color: var(--text-primary);
    }
    .solana-asset-btn:hover { border-color: var(--border-hover); }
    .solana-asset-btn.active { border-color: var(--text-accent); background: var(--bg-highlight); }
    .solana-asset-btn .asset-name { font-weight: 700; display: block; margin-bottom: 2px; text-transform: uppercase; letter-spacing: 0.04em; }
    .solana-asset-btn .asset-amount { font-size: 11px; color: var(--text-dim); font-family: var(--font-mono); }
    .solana-qr { text-align: center; margin: 16px 0; }
    .solana-qr img { width: 240px; height: 240px; aspect-ratio: 1/1; object-fit: contain; border-radius: 6px; background: #fff; padding: 8px; border: 1px solid var(--border-default); }
    .solana-amount { text-align: center; font-family: var(--font-mono); font-size: 18px; font-weight: 700; margin: 12px 0 4px; color: var(--text-primary); letter-spacing: -0.02em; }
    .solana-rate { text-align: center; font-family: var(--font-mono); font-size: 11px; color: var(--text-dim); margin-bottom: 12px; }
    .solana-timer { text-align: center; font-family: var(--font-mono); font-size: 11px; color: var(--text-dim); margin-bottom: 12px; }
    .solana-timer .timer-value { font-weight: 700; color: var(--text-primary); }
    .solana-wallet-btn.status-confirming { background: rgba(184,134,11,.1); border-color: rgba(184,134,11,.3); color: #b8860b; }
    .solana-wallet-btn.status-paid { background: rgba(46,125,50,.1); border-color: rgba(46,125,50,.3); color: #2e7d32; }
    .solana-wallet-btn.status-error { background: rgba(153,68,68,.1); border-color: var(--error-border); color: var(--error-text); }
    .solana-wallet-btn.status-expired { background: rgba(153,68,68,.1); border-color: var(--error-border); color: var(--error-text); }
    .solana-wallet-btn {
      display: flex; align-items: center; justify-content: center; gap: 8px;
      width: 100%; padding: 14px 20px; border-radius: 6px;
      font-family: var(--font-mono); font-size: 13px; font-weight: 700;
      text-transform: uppercase; letter-spacing: 0.04em;
      color: var(--bg-main); background: var(--text-accent);
      border: 1px solid var(--text-accent); cursor: pointer;
      transition: background 0.2s ease, box-shadow 0.2s ease;
      min-height: 46px;
    }
    .solana-wallet-btn:hover { background: var(--text-accent-hover); box-shadow: 0 0 20px var(--glow); }
    .solana-wallet-btn:active { background: var(--text-accent); }
    .solana-wallet-btn:disabled { opacity: 0.4; cursor: not-allowed; }
    .solana-wallet-btn:disabled:hover { background: var(--text-accent); box-shadow: none; }
    .solana-retry-btn {
      width: 100%; padding: 12px; border-radius: 6px; border: 1px solid var(--border-default);
      background: transparent; color: var(--text-primary);
      font-family: var(--font-mono); font-size: 12px; font-weight: 700;
      text-transform: uppercase; letter-spacing: 0.04em;
      cursor: pointer; margin-top: 8px; transition: border-color 0.2s ease;
    }
    .solana-retry-btn:hover { border-color: var(--text-accent); }
    .solana-divider {
      text-align: center; font-family: var(--font-mono); font-size: 10px;
      color: var(--text-dim); margin: 10px 0; text-transform: uppercase; letter-spacing: 0.06em;
    }
    .solana-fallback { text-align: center; font-size: 11px; color: var(--text-dim); margin-top: 8px; word-break: break-all; font-family: var(--font-mono); }
    .solana-fallback a { color: var(--text-accent); text-decoration: underline; }
    @keyframes solanaSpin { to { transform: rotate(360deg); } }
    .solana-spinner { width: 24px; height: 24px; border: 2px solid var(--border-default); border-top-color: var(--text-accent); border-radius: 50%; animation: solanaSpin .7s linear infinite; margin: 0 auto 12px; }

    /* Payment details (address + amount copy) */
    .solana-pay-details {
      border: 1px solid var(--border-default); border-radius: 6px; padding: 12px 14px;
      margin: 12px 0; background: var(--bg-main);
    }
    .solana-detail-row {
      display: flex; align-items: center; justify-content: space-between; gap: 8px;
      padding: 6px 0;
    }
    .solana-detail-row + .solana-detail-row { border-top: 1px solid var(--border-default); }
    .solana-detail-label {
      font-family: var(--font-mono); font-size: 10px; font-weight: 600;
      text-transform: uppercase; letter-spacing: 0.06em; color: var(--text-dim);
      flex-shrink: 0; min-width: 70px;
    }
    .solana-detail-value {
      display: flex; align-items: center; gap: 6px; min-width: 0; flex: 1; justify-content: flex-end;
    }
    .solana-detail-value code {
      font-family: var(--font-mono); font-size: 11px; color: var(--text-primary);
      word-break: break-all; text-align: right; line-height: 1.3;
    }
    .solana-copy-btn {
      flex-shrink: 0; background: none; border: 1px solid var(--border-default);
      border-radius: 4px; padding: 4px; cursor: pointer; color: var(--text-dim);
      display: flex; align-items: center; justify-content: center;
      transition: border-color 0.15s ease, color 0.15s ease, background 0.15s ease;
    }
    .solana-copy-btn:hover { border-color: var(--text-accent); color: var(--text-accent); }
    .solana-copy-btn.copied { border-color: #2ecc71; color: #2ecc71; background: rgba(46,204,113,0.08); }

    /* Mobile wallet picker */
    .solana-mobile-wallets { display: flex; gap: 8px; margin-top: 8px; }
    .solana-mobile-wallets.hidden { display: none; }
    .solana-mobile-wallet-btn {
      flex: 1; display: flex; align-items: center; justify-content: center; gap: 8px;
      font-family: var(--font-sans); font-size: 13px; font-weight: 600;
      color: var(--text-primary); background: var(--bg-surface);
      border: 1px solid var(--border-default); border-radius: 8px;
      padding: 10px 14px; cursor: pointer;
      transition: border-color 0.15s ease, background 0.15s ease;
    }
    .solana-mobile-wallet-btn:hover { border-color: var(--text-accent); background: var(--bg-highlight); }
    .solana-mobile-wallet-btn svg { flex-shrink: 0; color: var(--text-accent); }

    /* Checkboxes */
    .form-checkbox { display: flex; align-items: flex-start; gap: 10px; margin-bottom: 10px; cursor: pointer; }
    .form-checkbox input { display: none; }
    .checkbox-box {
      width: 18px; height: 18px; border: 2px solid var(--border-default); border-radius: 4px;
      flex-shrink: 0; display: flex; align-items: center; justify-content: center;
      transition: border-color 0.15s ease, background 0.15s ease; margin-top: 1px;
    }
    .form-checkbox input:checked ~ .checkbox-box { border-color: var(--text-accent); background: var(--text-accent); }
    .checkbox-box svg { display: none; }
    .form-checkbox input:checked ~ .checkbox-box svg { display: block; }
    .checkbox-label { font-family: var(--font-sans); font-size: 13px; color: var(--text-primary); line-height: 1.4; }
    .checkbox-label a { color: var(--text-accent); text-decoration: underline; }

    /* Validation states */
    .form-input.valid { border-color: #2ecc71; }
    .form-input-wrap { position: relative; }
    .form-input-wrap .form-input { width: 100%; padding-right: 32px; }
    .input-check {
      position: absolute; right: 10px; top: 50%; transform: translateY(-50%);
      width: 16px; height: 16px; color: #2ecc71; display: none;
    }
    .form-input.valid ~ .input-check { display: block; }

    /* Phone prefix */
    .phone-wrap { display: flex; gap: 8px; }
    .phone-prefix {
      min-width: 80px; flex-shrink: 0;
      font-family: var(--font-mono); font-size: 14px; color: var(--text-primary);
      background: var(--bg-surface); border: 1px solid var(--border-default); border-radius: 6px;
      padding: 10px 10px; text-align: center; white-space: nowrap;
      display: flex; align-items: center; justify-content: center; gap: 4px;
    }
    .phone-wrap .form-input { flex: 1; }

    /* Card type highlight */
    .pay-method-icons span.detected { border-color: var(--text-accent); color: var(--text-accent); background: var(--bg-main); }

    /* Email suggestion */
    .email-suggestion { font-family: var(--font-sans); font-size: 12px; color: var(--text-dim); margin-top: 4px; cursor: pointer; }
    .email-suggestion span { color: var(--text-accent); text-decoration: underline; cursor: pointer; }

    /* Error micro-copy */
    .field-error-msg { font-family: var(--font-sans); font-size: 12px; color: #e74c3c; margin-top: 3px; display: none; }
    .form-input.error ~ .field-error-msg,
    .form-group.has-error .field-error-msg { display: block; }

    /* Estimated delivery */
    .delivery-estimate {
      display: flex; align-items: center; justify-content: center; gap: 8px;
      padding: 10px 12px; background: transparent; border: none; border-radius: 6px;
      margin-top: 8px; font-family: var(--font-sans); font-size: 13px; color: var(--text-primary);
    }
    .delivery-estimate svg { width: 16px; height: 16px; color: var(--text-accent); flex-shrink: 0; }
    .delivery-estimate strong { color: var(--text-accent); }

    /* Trust badges */
    .trust-badges { display: flex; justify-content: center; gap: 20px; margin-top: 16px; flex-wrap: wrap; }
    .trust-badge { display: flex; align-items: center; gap: 5px; font-family: var(--font-sans); font-size: 11px; font-weight: 400; color: var(--text-dim); }
    .trust-badge svg { width: 13px; height: 13px; color: var(--text-dim); flex-shrink: 0; opacity: 0.5; }

    /* Checkout button */
    .order-checkout-btn {
      display: flex; align-items: center; justify-content: center; gap: 8px;
      width: 100%; padding: 14px 20px; margin-top: 20px;
      font-family: var(--font-mono); font-size: 13px; font-weight: 700;
      text-transform: uppercase; letter-spacing: 0.04em;
      color: var(--bg-main); background: var(--text-accent);
      border: 1px solid var(--text-accent); border-radius: 6px;
      cursor: pointer; transition: background 0.2s ease, box-shadow 0.2s ease;
      min-height: 46px; text-decoration: none;
    }
    .order-checkout-btn:hover { background: var(--text-accent-hover); box-shadow: 0 0 20px var(--glow); }
    .order-checkout-btn svg { flex-shrink: 0; }
    .order-checkout-btn:disabled { opacity: 0.4; cursor: not-allowed; }
    .order-checkout-btn:disabled:hover { background: var(--text-accent); box-shadow: none; }

    /* ===== RIGHT: ORDER SUMMARY SIDEBAR ===== */
    .order-sidebar { position: sticky; top: calc(var(--header-h) + 24px); }
    .order-summary { background: var(--bg-surface); border: 1px solid var(--border-default); border-radius: 8px; padding: 24px; }
    .order-summary-title {
      font-family: var(--font-mono); font-size: 11px; font-weight: 700;
      text-transform: uppercase; letter-spacing: 0.06em; color: var(--text-dim);
      margin-bottom: 20px; padding-bottom: 12px; border-bottom: 1px solid var(--border-default);
    }
    .cart-item { display: flex; gap: 14px; padding: 14px 16px; margin-bottom: 14px; background: var(--bg-surface); border: 1px solid var(--border-default); border-radius: 10px; }
    .cart-details { flex: 1; min-width: 0; }
    .cart-product-name { font-family: var(--font-sans); font-size: 15px; font-weight: 700; color: var(--text-primary); margin-bottom: 10px; letter-spacing: -0.01em; }
    .cart-config-line { display: flex; align-items: center; justify-content: space-between; gap: 8px; font-family: var(--font-sans); font-size: 13px; color: var(--text-dim); line-height: 1.4; padding: 5px 0; }
    .cart-config-line span { color: var(--text-primary); font-weight: 600; }
    .cart-config-qty { display: inline-flex; align-items: center; justify-content: center; min-width: 20px; font-family: var(--font-mono); font-size: 10px; font-weight: 700; color: var(--text-accent); background: var(--bg-highlight); border-radius: 5px; padding: 2px 6px; letter-spacing: 0; flex-shrink: 0; }
    .cart-config-label { color: var(--text-primary); font-weight: 500; font-size: 13px; }
    .cart-config-value { color: var(--text-dim); font-weight: 500; font-size: 13px; margin-left: auto; text-transform: capitalize; }
    .cart-config-value.is-yes { color: var(--text-primary); font-weight: 600; }
    .cart-config-value.is-no { color: var(--text-dim); opacity: 0.5; }
    .cart-config-divider { height: 1px; background: var(--border-default); margin: 4px 0; opacity: 0.35; }
    .summary-row { display: flex; justify-content: space-between; align-items: center; padding: 3px 0; font-size: 13px; }
    .summary-key { font-family: var(--font-sans); color: var(--text-dim); font-weight: 500; }
    .summary-val { font-family: var(--font-sans); color: var(--text-accent); font-weight: 600; }
    .summary-val-free { font-family: var(--font-sans); color: var(--text-dim); font-weight: 600; }
    .summary-val-add { font-family: var(--font-sans); color: var(--text-label); font-weight: 600; }
    .summary-divider { height: 1px; background: var(--border-default); margin: 12px 0; }
    .summary-total .summary-key, .summary-total .summary-val { font-size: 16px; color: var(--text-accent); font-weight: 700; }

    .order-notes { margin-top: 16px; display: flex; flex-direction: column; gap: 6px; }
    .order-note { display: flex; align-items: center; gap: 8px; font-family: var(--font-sans); font-size: 12px; font-weight: 400; color: var(--text-dim); line-height: 1.4; }
    .order-note svg { width: 14px; height: 14px; flex-shrink: 0; color: var(--text-dim); }

    /* Promo code */
    .promo-section { margin-top: 12px; }
    .promo-form { display: flex; gap: 8px; }
    .promo-input {
      flex: 1; font-family: var(--font-mono); font-size: 13px; color: var(--text-primary);
      background: var(--bg-main); border: 1px solid var(--border-default); border-radius: 6px;
      padding: 8px 12px; outline: none; text-transform: uppercase; letter-spacing: 0.04em;
    }
    .promo-input:focus { border-color: var(--text-accent); }
    .promo-apply {
      font-family: var(--font-mono); font-size: 11px; font-weight: 700;
      text-transform: uppercase; letter-spacing: 0.04em;
      color: var(--bg-main); background: var(--text-accent);
      border: none; border-radius: 6px; padding: 8px 16px; cursor: pointer; white-space: nowrap;
    }
    .promo-remove {
      font-size: 18px; font-weight: 700; color: #e74c3c;
      background: transparent; border: 1px solid #e74c3c; border-radius: 6px;
      padding: 4px 12px; cursor: pointer; line-height: 1; transition: background 0.15s, color 0.15s;
    }
    .promo-remove:hover { background: #e74c3c; color: #fff; }
    .promo-input:disabled { opacity: 1; cursor: not-allowed; color: #1a7f37; background: #e6f9ed; border-color: #2ecc71; }
    .promo-msg { font-family: var(--font-sans); font-size: 12px; margin-top: 4px; display: none; transition: opacity 0.2s; }
    .promo-msg.success { color: #2ecc71; display: block; }
    .promo-msg.error { color: #e74c3c; display: block; }
    .shipping-val-dynamic { color: var(--text-label); font-weight: 600; }

    /* Sticky mobile CTA */
    .mobile-sticky-cta {
      display: none; position: fixed; bottom: 0; left: 0; right: 0; z-index: 100;
      background: var(--bg-surface); border-top: 1px solid var(--border-default);
      padding: 16px 20px calc(16px + env(safe-area-inset-bottom, 0px));
      align-items: center; justify-content: space-between; gap: 16px;
    }
    .mobile-sticky-price { font-family: var(--font-mono); font-size: 20px; font-weight: 700; color: var(--text-accent); }
    .mobile-sticky-btn {
      flex: 1; max-width: 220px;
      display: flex; align-items: center; justify-content: center;
      padding: 14px 20px;
      font-family: var(--font-mono); font-size: 13px; font-weight: 700;
      text-transform: uppercase; letter-spacing: 0.04em;
      color: var(--bg-main); background: var(--text-accent);
      border: none; border-radius: 8px; cursor: pointer; min-height: 48px;
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width: 860px) {
      .checkout-grid { grid-template-columns: 1fr; gap: 32px; }
      .order-sidebar { position: static; }
      .mobile-sticky-cta { display: flex; }
      .checkout-page { padding-bottom: 120px; }
      .trust-badges { gap: 12px; margin-top: 12px; }
      .trust-badge { font-size: 10px; gap: 3px; }
      .trust-badge svg { width: 11px; height: 11px; }
    }
    @media (max-width: 520px) {
      .checkout-page { padding: 32px 0 120px; }
      .form-row { grid-template-columns: 1fr; }
      .card-fields .form-row:not(.full) { grid-template-columns: 1fr 1fr; }
      .form-input { font-size: 16px; }
      .trust-badges { gap: 6px; margin-top: 8px; }
      .trust-badge { font-size: 9px; gap: 3px; }
      .trust-badge svg { width: 10px; height: 10px; }
    }

    /* === UA FLOW === */
    .ua-check-label { display: flex; align-items: center; gap: 8px; font-family: var(--font-body); font-size: 14px; font-weight: 500; color: var(--text-label); cursor: pointer; padding: 6px 0; }
    .ua-check-label input { display: none; }
    .ua-check-box { width: 20px; height: 20px; border-radius: 4px; border: 2px solid var(--border-default); display: flex; align-items: center; justify-content: center; flex-shrink: 0; transition: all 0.15s ease; }
    .ua-check-box svg { width: 14px; height: 14px; display: none; }
    .ua-check-label input:checked ~ .ua-check-box { background: var(--text-accent); border-color: var(--text-accent); }
    .ua-check-label input:checked ~ .ua-check-box svg { display: block; color: #fff; }
    .ua-active #countryRow { display: none; }
    .ua-delivery { display: none; }
    .ua-flow .ua-delivery { display: block; }
    .ua-flow .standard-delivery { display: none; }
    .ua-pay-methods { display: none; }
    .ua-flow-pay .ua-pay-methods { display: flex; flex-direction: column; gap: 8px; margin-bottom: 16px; }
    .ua-flow-pay .pay-methods { display: none; }
    .ua-flow-pay .card-fields { display: none !important; }
    .np-suggestions {
      position: absolute; top: 100%; left: 0; right: 0; z-index: 10;
      background: var(--bg-main); border: 1px solid var(--border-default);
      border-top: none; border-radius: 0 0 6px 6px;
      max-height: 200px; overflow-y: auto; display: none;
    }
    .np-suggestions.open { display: block; }
    .np-suggestion { padding: 8px 12px; font-family: var(--font-sans); font-size: 14px; cursor: pointer; border-bottom: 1px solid var(--border-default); }
    .np-suggestion:last-child { border-bottom: none; }
    .np-suggestion:hover { background: var(--bg-surface-hover); }
  </style>
</head>
<body>
  <%- include('../partials/gtm-body') %>
  <noscript><p style="padding:40px;text-align:center;font-size:16px;">JavaScript is required.</p></noscript>
  <%- include('../partials/header') %>

  <main class="checkout-page" role="main" data-section="checkout">
    <div class="container">

      <a href="<%= langPrefix %>/order" class="order-back">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5"/><path d="M12 19l-7-7 7-7"/></svg>
        <%= t.order.backToConfig || t.order.backToProduct %>
      </a>

      <div class="checkout-grid">

        <!-- ===== LEFT: CHECKOUT STEPS ===== -->
        <div class="checkout-forms">

          <!-- ===== STEP 1: CONTACT INFO ===== -->
          <div class="checkout-step active" id="stepContact" data-section="checkout">
            <div class="step-header">
              <div class="step-header-left">
                <span class="step-number">1</span>
                <span class="step-title"><%= t.order.progressContact %></span>
              </div>
              <button class="step-edit-btn" id="editContact" type="button"><%= t.order.stepEdit %></button>
            </div>
            <div class="step-summary" id="contactSummary"></div>
            <div class="step-body">
              <div class="form-privacy">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
                <span><%= t.order.privacyNote %> <a href="<%= langPrefix %>/cookie-policy" style="color:var(--text-accent);text-decoration:underline"><%= t.order.privacyLearnMore %></a></span>
              </div>
              <form id="contactForm" autocomplete="on" novalidate>
                <div class="form-row">
                  <div class="form-group">
                    <div class="form-input-wrap">
                      <input class="form-input form-input-lg" type="text" id="firstName" name="given-name" autocomplete="given-name" placeholder="<%= t.order.firstName %>" aria-describedby="firstNameError" required>
                      <svg class="input-check" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg>
                    </div>
                    <div class="field-error-msg" id="firstNameError" role="alert"></div>
                  </div>
                  <div class="form-group">
                    <div class="form-input-wrap">
                      <input class="form-input form-input-lg" type="text" id="lastName" name="family-name" autocomplete="family-name" placeholder="<%= t.order.lastName %>" aria-describedby="lastNameError" required>
                      <svg class="input-check" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg>
                    </div>
                    <div class="field-error-msg" id="lastNameError" role="alert"></div>
                  </div>
                </div>
                <div class="form-row full">
                  <div class="form-group">
                    <div class="form-input-wrap">
                      <input class="form-input form-input-lg" type="email" id="email" name="email" autocomplete="email" inputmode="email" placeholder="<%= t.order.email %>" aria-describedby="emailError" required>
                      <svg class="input-check" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg>
                    </div>
                    <div class="email-suggestion" id="emailSuggestion"></div>
                    <div class="field-error-msg" id="emailError" role="alert"></div>
                  </div>
                </div>
                <div class="form-row full">
                  <div class="form-group">
                    <label class="ua-check-label" id="uaCheckLabel">
                      <input type="checkbox" id="uaCheck">
                      <span class="ua-check-box"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg></span>
                      <span><%= t.order.isUkraine || 'Is it Ukraine?' %> &#127482;&#127462;</span>
                    </label>
                  </div>
                </div>
                <div class="form-row full" id="countryRow">
                  <div class="form-group">
                    <label class="form-label" for="country"><%= t.order.countryOfDelivery || 'Country of delivery' %></label>
                    <select class="form-input" id="country" name="country" autocomplete="country-name" required>
                      <option value="" disabled selected><%= t.order.countryPlaceholder %></option>
                      <option value="AT">&#127462;&#127481; Austria</option>
                      <option value="BE">&#127463;&#127466; Belgium</option>
                      <option value="BG">&#127463;&#127468; Bulgaria</option>
                      <option value="HR">&#127469;&#127479; Croatia</option>
                      <option value="CY">&#127464;&#127486; Cyprus</option>
                      <option value="CZ">&#127464;&#127487; Czech Republic</option>
                      <option value="DK">&#127465;&#127472; Denmark</option>
                      <option value="EE">&#127466;&#127466; Estonia</option>
                      <option value="FI">&#127467;&#127470; Finland</option>
                      <option value="FR">&#127467;&#127479; France</option>
                      <option value="DE">&#127465;&#127466; Germany</option>
                      <option value="GR">&#127468;&#127479; Greece</option>
                      <option value="HU">&#127469;&#127482; Hungary</option>
                      <option value="IE">&#127470;&#127466; Ireland</option>
                      <option value="IT">&#127470;&#127481; Italy</option>
                      <option value="LV">&#127473;&#127483; Latvia</option>
                      <option value="LT">&#127473;&#127481; Lithuania</option>
                      <option value="LU">&#127473;&#127482; Luxembourg</option>
                      <option value="MT">&#127474;&#127481; Malta</option>
                      <option value="NL">&#127475;&#127473; Netherlands</option>
                      <option value="PL">&#127477;&#127473; Poland</option>
                      <option value="PT">&#127477;&#127481; Portugal</option>
                      <option value="RO">&#127479;&#127476; Romania</option>
                      <option value="SK">&#127480;&#127472; Slovakia</option>
                      <option value="SI">&#127480;&#127470; Slovenia</option>
                      <option value="ES">&#127466;&#127480; Spain</option>
                      <option value="SE">&#127480;&#127466; Sweden</option>
                      <option value="GB">&#127468;&#127463; United Kingdom</option>
                      <option value="US">&#127482;&#127480; United States</option>
                      <option value="CA">&#127464;&#127462; Canada</option>
                      <option value="AU">&#127462;&#127482; Australia</option>
                      <option value="JP">&#127471;&#127477; Japan</option>
                      <option value="CH">&#127464;&#127469; Switzerland</option>
                      <option value="NO">&#127475;&#127476; Norway</option>
                      <option value="UA">&#127482;&#127462; Ukraine</option>
                    </select>
                  </div>
                </div>
                <div class="form-row full">
                  <div class="form-group">
                    <div class="phone-wrap">
                      <span class="phone-prefix" id="phonePrefix">+</span>
                      <input class="form-input" type="tel" id="phone" name="phone" autocomplete="tel-national" inputmode="tel" placeholder="<%= t.order.phone %>" aria-describedby="phoneError" required>
                    </div>
                    <div class="field-error-msg" id="phoneError" role="alert"></div>
                  </div>
                </div>
                <button class="step-continue-btn" id="contactContinue" type="submit"><%= t.order.deliveryAddress || 'Continue to Delivery' %></button>
              </form>
            </div>
          </div>

          <!-- ===== STEP 2: DELIVERY ADDRESS ===== -->
          <div class="checkout-step disabled" id="stepDelivery" data-section="checkout">
            <div class="step-header">
              <div class="step-header-left">
                <span class="step-number">2</span>
                <span class="step-title"><%= t.order.deliveryAddress %></span>
              </div>
              <button class="step-edit-btn" id="editDelivery" type="button"><%= t.order.stepEdit %></button>
            </div>
            <div class="step-summary" id="deliverySummary"></div>
            <div class="step-body">
              <form id="deliveryForm" autocomplete="on" novalidate>
                <!-- Standard international delivery fields -->
                <div class="standard-delivery">
                  <div class="form-row full">
                    <div class="form-group">
                      <input class="form-input form-input-lg" type="text" id="street" name="address-line1" autocomplete="address-line1" placeholder="<%= t.order.street %>" aria-describedby="streetError" required>
                      <div class="field-error-msg" id="streetError" role="alert"></div>
                    </div>
                  </div>
                  <div class="form-row">
                    <div class="form-group">
                      <input class="form-input form-input-sm" type="text" id="apt" name="address-line2" autocomplete="address-line2" placeholder="<%= t.order.apt %>">
                    </div>
                    <div class="form-group">
                      <input class="form-input form-input-sm" type="text" id="zip" name="postal-code" autocomplete="postal-code" inputmode="text" placeholder="<%= t.order.zip %>" aria-describedby="zipError" required>
                      <div class="field-error-msg" id="zipError" role="alert"></div>
                      <div class="zip-suggestions" id="zipSuggestions"></div>
                    </div>
                  </div>
                  <div class="form-row full">
                    <div class="form-group">
                      <input class="form-input" type="text" id="city" name="city" autocomplete="address-level2" placeholder="<%= t.order.city %>" aria-describedby="cityError" required>
                      <div class="field-error-msg" id="cityError" role="alert"></div>
                    </div>
                  </div>
                </div>
                <!-- UA: Nova Poshta delivery -->
                <div class="ua-delivery">
                  <div class="form-row full">
                    <div class="form-group" style="position:relative">
                      <input class="form-input" type="text" id="npCity" placeholder="<%= t.order.npCity || 'City' %>" autocomplete="off">
                      <div class="np-suggestions" id="npCitySuggestions"></div>
                    </div>
                  </div>
                  <div class="form-row full">
                    <div class="form-group">
                      <select class="form-input" id="npWarehouse" disabled>
                        <option value="" disabled selected><%= t.order.npWarehouse || 'Nova Poshta department' %></option>
                      </select>
                      <div class="field-error-msg" id="npWarehouseError" role="alert"></div>
                    </div>
                  </div>
                </div>
                <button class="step-continue-btn" id="deliveryContinue" type="submit"><%= t.order.continueToPayment %></button>
                <div class="delivery-estimate" id="deliveryEstimate" style="display:none">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="3" width="15" height="13"/><polygon points="16 8 20 8 23 11 23 16 16 16 16 8"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
                  <span id="deliveryText"></span>
                </div>
              </form>
            </div>
          </div>

          <!-- ===== STEP 3: PAYMENT ===== -->
          <div class="checkout-step disabled" id="stepPayment" data-section="checkout">
            <div class="step-header">
              <div class="step-header-left">
                <span class="step-number">3</span>
                <span class="step-title"><%= t.order.stepPaymentTitle %></span>
              </div>
            </div>
            <div class="step-body">
              <div class="pay-methods" id="payMethods" role="radiogroup" aria-label="<%= t.order.stepPaymentTitle %>">
                <label class="pay-method" data-method="card">
                  <input type="radio" name="payMethod" value="card">
                  <span class="pay-radio"><span class="pay-radio-dot"></span></span>
                  <svg class="pay-method-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="4" width="22" height="16" rx="2" ry="2"/><line x1="1" y1="10" x2="23" y2="10"/></svg>
                  <span class="pay-method-info">
                    <span class="pay-method-name"><%= t.order.payCard %></span>
                  </span>
                  <span class="pay-method-icons" id="cardTypeIcons">
                    <span data-card="visa">VISA</span>
                    <span data-card="mc">MC</span>
                    <span data-card="amex">AMEX</span>
                  </span>
                </label>
                <label class="pay-method" data-method="apple">
                  <input type="radio" name="payMethod" value="apple">
                  <span class="pay-radio"><span class="pay-radio-dot"></span></span>
                  <svg class="pay-method-icon" viewBox="0 0 24 24" fill="currentColor"><path d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.8-.91.65.03 2.47.26 3.64 1.98-.09.06-2.17 1.28-2.15 3.81.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M13 3.5c.73-.83 1.94-1.46 2.94-1.5.13 1.17-.34 2.35-1.04 3.19-.69.85-1.83 1.51-2.95 1.42-.15-1.15.41-2.35 1.05-3.11"/></svg>
                  <span class="pay-method-info">
                    <span class="pay-method-name">Apple Pay</span>
                  </span>
                </label>
                <label class="pay-method" data-method="google">
                  <input type="radio" name="payMethod" value="google">
                  <span class="pay-radio"><span class="pay-radio-dot"></span></span>
                  <svg class="pay-method-icon" viewBox="0 0 24 24"><path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92a5.06 5.06 0 0 1-2.2 3.32v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.1z" fill="#4285F4"/><path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853"/><path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z" fill="#FBBC05"/><path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335"/></svg>
                  <span class="pay-method-info">
                    <span class="pay-method-name">Google Pay</span>
                  </span>
                </label>
                <label class="pay-method active" data-method="crypto">
                  <input type="radio" name="payMethod" value="crypto" checked>
                  <span class="pay-radio"><span class="pay-radio-dot"></span></span>
                  <svg class="pay-method-icon" viewBox="0 0 397.7 311.7" fill="currentColor"><path d="M64.6 237.9c2.4-2.4 5.7-3.8 9.2-3.8h317.4c5.8 0 8.7 7 4.6 11.1l-62.7 62.7c-2.4 2.4-5.7 3.8-9.2 3.8H6.5c-5.8 0-8.7-7-4.6-11.1l62.7-62.7z"/><path d="M64.6 3.8C67.1 1.4 70.4 0 73.8 0h317.4c5.8 0 8.7 7 4.6 11.1l-62.7 62.7c-2.4 2.4-5.7 3.8-9.2 3.8H6.5c-5.8 0-8.7-7-4.6-11.1L64.6 3.8z"/><path d="M333.1 120.1c-2.4-2.4-5.7-3.8-9.2-3.8H6.5c-5.8 0-8.7 7-4.6 11.1l62.7 62.7c2.4 2.4 5.7 3.8 9.2 3.8h317.4c5.8 0 8.7-7 4.6-11.1l-62.7-62.7z"/></svg>
                  <span class="pay-method-info">
                    <span class="pay-method-name"><%= t.order.payCrypto %></span>
                    <span class="pay-method-desc"><%= t.order.payCryptoDesc %></span>
                  </span>
                </label>
              </div>

              <!-- UA payment methods -->
              <div class="ua-pay-methods" id="uaPayMethods" role="radiogroup" aria-label="Тип оплати">
                <label class="pay-method" data-method="ua-card">
                  <input type="radio" name="uaPayMethod" value="ua-card">
                  <span class="pay-radio"><span class="pay-radio-dot"></span></span>
                  <svg class="pay-method-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="4" width="22" height="16" rx="2" ry="2"/><line x1="1" y1="10" x2="23" y2="10"/></svg>
                  <span class="pay-method-info"><span class="pay-method-name"><%= t.order.payUaCard || 'Card transfer' %></span></span>
                </label>
                <label class="pay-method" data-method="ua-invoice">
                  <input type="radio" name="uaPayMethod" value="ua-invoice">
                  <span class="pay-radio"><span class="pay-radio-dot"></span></span>
                  <svg class="pay-method-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
                  <span class="pay-method-info"><span class="pay-method-name"><%= t.order.payUaInvoice || 'Invoice payment' %></span></span>
                </label>
                <label class="pay-method active" data-method="ua-cod">
                  <input type="radio" name="uaPayMethod" value="ua-cod" checked>
                  <span class="pay-radio"><span class="pay-radio-dot"></span></span>
                  <svg class="pay-method-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="3" width="15" height="13"/><polygon points="16 8 20 8 23 11 23 16 16 16 16 8"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
                  <span class="pay-method-info"><span class="pay-method-name"><%= t.order.payUaCod || 'Cash on delivery' %></span><span class="pay-method-desc"><%= t.order.payUaCodDesc || 'Pay on pickup' %></span></span>
                </label>
                <label class="pay-method" data-method="crypto">
                  <input type="radio" name="uaPayMethod" value="crypto">
                  <span class="pay-radio"><span class="pay-radio-dot"></span></span>
                  <svg class="pay-method-icon" viewBox="0 0 397.7 311.7" fill="currentColor"><path d="M64.6 237.9c2.4-2.4 5.7-3.8 9.2-3.8h317.4c5.8 0 8.7 7 4.6 11.1l-62.7 62.7c-2.4 2.4-5.7 3.8-9.2 3.8H6.5c-5.8 0-8.7-7-4.6-11.1l62.7-62.7z"/><path d="M64.6 3.8C67.1 1.4 70.4 0 73.8 0h317.4c5.8 0 8.7 7 4.6 11.1l-62.7 62.7c-2.4 2.4-5.7 3.8-9.2 3.8H6.5c-5.8 0-8.7-7-4.6-11.1L64.6 3.8z"/><path d="M333.1 120.1c-2.4-2.4-5.7-3.8-9.2-3.8H6.5c-5.8 0-8.7 7-4.6 11.1l62.7 62.7c2.4 2.4 5.7 3.8 9.2 3.8h317.4c5.8 0 8.7-7 4.6-11.1l-62.7-62.7z"/></svg>
                  <span class="pay-method-info"><span class="pay-method-name"><%= t.order.payCrypto || 'Crypto (SOL / USDC)' %></span><span class="pay-method-desc"><%= t.order.payCryptoDesc || 'Solana blockchain' %></span></span>
                </label>
              </div>

              <!-- Solana Pay checkout container -->
              <div class="solana-checkout hidden" id="solanaCheckout">
                <div class="solana-asset-select" id="assetSelect">
                  <button type="button" class="solana-asset-btn active" data-asset="SOL">
                    <span class="asset-name">SOL</span>
                    <span class="asset-amount" id="solAmount">—</span>
                  </button>
                  <button type="button" class="solana-asset-btn" data-asset="USDC">
                    <span class="asset-name">USDC</span>
                    <span class="asset-amount" id="usdcAmount">—</span>
                  </button>
                </div>
                <div class="solana-timer" id="solanaTimer"></div>
                <div class="solana-qr" id="solanaQr"></div>
                <div class="solana-amount" id="solanaAmountDisplay"></div>
                <div class="solana-rate" id="solanaRate"></div>

                <!-- Payment details: recipient address + exact amount with copy -->
                <div class="solana-pay-details" id="solanaPayDetails" style="display:none">
                  <div class="solana-detail-row">
                    <span class="solana-detail-label"><%= t.order.cryptoRecipient || 'Recipient' %></span>
                    <div class="solana-detail-value">
                      <code id="solanaRecipientAddr"></code>
                      <button type="button" class="solana-copy-btn" data-copy="solanaRecipientAddr" title="Copy">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
                      </button>
                    </div>
                  </div>
                  <div class="solana-detail-row">
                    <span class="solana-detail-label"><%= t.order.cryptoExactAmount || 'Amount' %></span>
                    <div class="solana-detail-value">
                      <code id="solanaExactAmount"></code>
                      <button type="button" class="solana-copy-btn" data-copy="solanaExactAmount" title="Copy">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
                      </button>
                    </div>
                  </div>
                </div>

                <button type="button" class="solana-wallet-btn" id="solanaWalletBtn">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="6" width="20" height="14" rx="2"/><path d="M2 10h20"/><path d="M16 14h2"/></svg>
                  <%= t.order.cryptoOpenWallet || 'Pay with Wallet' %>
                </button>

                <!-- Mobile wallet picker -->
                <div class="solana-mobile-wallets hidden" id="mobileWalletPicker">
                  <button type="button" class="solana-mobile-wallet-btn" data-wallet="phantom">
                    <svg width="20" height="20" viewBox="0 0 128 128" fill="currentColor"><path d="M110.6 57.4C107.3 33.7 87.3 16 64 16 40.7 16 20.7 33.7 17.4 57.4 8.5 59.8 2 68.2 2 78c0 11.6 8.4 21 18.8 21H64h43.2c10.4 0 18.8-9.4 18.8-21 0-9.8-6.5-18.2-15.4-20.6zM44 72c-4.4 0-8-3.6-8-8s3.6-8 8-8 8 3.6 8 8-3.6 8-8 8zm40 0c-4.4 0-8-3.6-8-8s3.6-8 8-8 8 3.6 8 8-3.6 8-8 8z"/></svg>
                    Phantom
                  </button>
                  <button type="button" class="solana-mobile-wallet-btn" data-wallet="solflare">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><circle cx="12" cy="12" r="10"/><path d="M12 6l4 6-4 6-4-6z" fill="white"/></svg>
                    Solflare
                  </button>
                </div>

                <div class="solana-divider"><%= t.order.cryptoOrScan || 'or scan QR with your mobile wallet' %></div>
              </div>

              <!-- Card fields -->
              <div class="card-fields hidden" id="cardFields">
                <div class="form-row full">
                  <div class="form-group">
                    <label class="form-label" for="cardNumber"><%= t.order.cardNumber %></label>
                    <input class="form-input" type="text" id="cardNumber" inputmode="numeric" placeholder="1234 5678 9012 3456" autocomplete="cc-number">
                  </div>
                </div>
                <div class="form-row">
                  <div class="form-group">
                    <label class="form-label" for="cardExpiry"><%= t.order.cardExpiry %></label>
                    <input class="form-input" type="text" id="cardExpiry" inputmode="numeric" placeholder="MM/YY" autocomplete="cc-exp">
                  </div>
                  <div class="form-group">
                    <label class="form-label" for="cardCvc"><%= t.order.cardCvc %></label>
                    <input class="form-input" type="text" id="cardCvc" inputmode="numeric" placeholder="123" autocomplete="cc-csc">
                  </div>
                </div>
                <div class="form-row full">
                  <div class="form-group">
                    <label class="form-label" for="cardName"><%= t.order.cardName %></label>
                    <input class="form-input" type="text" id="cardName" autocomplete="cc-name">
                  </div>
                </div>
              </div>

              <!-- Checkboxes -->
              <div style="margin-top:16px" id="checkboxesArea">
                <label class="form-checkbox">
                  <input type="checkbox" id="agreeTerms" required>
                  <span class="checkbox-box">
                    <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg>
                  </span>
                  <span class="checkbox-label"><%- t.order.agreeTerms %></span>
                </label>
                <label class="form-checkbox">
                  <input type="checkbox" id="newsletter">
                  <span class="checkbox-box">
                    <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg>
                  </span>
                  <span class="checkbox-label"><%= t.order.newsletter %></span>
                </label>
              </div>

              <button class="order-checkout-btn" id="checkoutBtn" type="button" style="margin-top:16px" disabled
                data-track="checkout_start" data-cta-id="order_checkout" data-section="checkout" data-product-id="penger-v1">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="4" width="22" height="16" rx="2" ry="2"/><line x1="1" y1="10" x2="23" y2="10"/></svg>
                <span id="checkoutBtnText"><%= t.order.payNow %></span>
              </button>
              <div class="trust-badges">
                <span class="trust-badge">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
                  <%= t.order.trustSecure %>
                </span>
                <span class="trust-badge">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
                  <%= t.order.trustSSL %>
                </span>
                <span class="trust-badge">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><path d="M22 4L12 14.01l-3-3"/></svg>
                  <%= t.order.trustGuarantee %>
                </span>
              </div>
            </div>
          </div>

        </div>

        <!-- ===== RIGHT: ORDER SUMMARY ===== -->
        <div class="order-sidebar">
          <div class="order-summary" data-section="checkout">
            <p class="order-summary-title"><%= t.order.summaryLabel %></p>

            <!-- Cart item -->
            <div class="cart-item">
              <div class="cart-details">
                <div class="cart-product-name"><%= t.order.productName %></div>
                <div id="cartConfigLines"
                     data-plate-label="<%= t.order.cartTitaniumPlate %>"
                     data-sleeve-label="<%= t.order.cartSleeve %>"
                     data-punch-label="<%= t.order.cartPunchTool %>"
                     data-yes="<%= t.order.cartYes %>"
                     data-no="<%= t.order.cartNo %>">
                </div>
              </div>
            </div>

            <!-- Price breakdown -->
            <div class="summary-row">
              <span class="summary-key"><%= t.order.priceLabel %></span>
              <span class="summary-val"><%= t.order.priceValue %></span>
            </div>
            <div class="summary-row" id="rowExtraPlates" style="display:none">
              <span class="summary-key" id="extraPlatesLabel"></span>
              <span class="summary-val-add" id="extraPlatesPrice"></span>
            </div>
            <div class="summary-row" id="rowSleeve">
              <span class="summary-key" id="sleeveLabel">Sleeve</span>
              <span class="summary-val-add" id="sleevePrice">+&euro;10</span>
            </div>
            <div class="summary-row" id="rowPunch">
              <span class="summary-key" id="punchLabel">Punch tool</span>
              <span class="summary-val-add" id="punchPrice">+&euro;10</span>
            </div>
            <div class="summary-row" id="rowShippingFree">
              <span class="summary-key"><%= t.order.shippingLabel %></span>
              <span class="summary-val-free" id="shippingFreeVal"><%= t.order.shippingValue %></span>
            </div>

            <div class="summary-row" id="rowShippingCost" style="display:none">
              <span class="summary-key"><%= t.order.shippingLabel %></span>
              <span class="summary-val-add" id="shippingCostVal"></span>
            </div>
            <div class="summary-row" id="rowDiscount" style="display:none">
              <span class="summary-key" id="discountLabel"><%= t.order.promoDiscount %></span>
              <span class="summary-val" id="discountVal" style="color:#2ecc71"></span>
            </div>

            <div class="summary-divider"></div>

            <div class="summary-row summary-total">
              <span class="summary-key"><%= t.order.totalLabel %></span>
              <span class="summary-val" id="summaryTotal" aria-live="polite">&euro;69</span>
            </div>

            <div class="promo-section">
              <div class="promo-form open" id="promoForm">
                <input class="promo-input" type="text" id="promoInput" placeholder="<%= t.order.promoPlaceholder %>" maxlength="20">
                <button class="promo-apply" id="promoApply" type="button"><%= t.order.promoApply %></button>
                <button class="promo-remove" id="promoRemove" type="button" style="display:none" aria-label="<%= t.order.promoRemove || 'Remove' %>">&times;</button>
              </div>
              <div class="promo-msg" id="promoMsg"></div>
            </div>

            <div class="order-notes">
              <span class="order-note">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
                <%= t.order.preorderNote %>
              </span>
              <span class="order-note">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
                <%= t.order.warrantyNote %>
              </span>
              <span class="order-note">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
                <%= t.order.secureNote %>
              </span>
            </div>
          </div>
        </div>

      </div>
    </div>
  </main>

  <!-- Sticky mobile CTA -->
  <div class="mobile-sticky-cta" id="mobileCta">
    <span class="mobile-sticky-price" id="mobilePrice">&euro;69</span>
    <button class="mobile-sticky-btn" id="mobileContinueBtn" type="button"><%= t.order.continueToPayment %></button>
  </div>

  <%- include('../partials/footer') %>

  <script>
  window.ORDER_I18N = <%- JSON.stringify({
    platesDesc: t.order.platesDesc,
    langPrefix: langPrefix,
    stepEdit: t.order.stepEdit,
    firstName: t.order.firstName,
    lastName: t.order.lastName,
    email: t.order.email,
    phone: t.order.phone,
    country: t.order.country,
    continueToDelivery: t.order.deliveryAddress || "Continue to Delivery",
    continueToPayment: t.order.continueToPayment,
    payNow: t.order.payNow,
    emailDidYouMean: t.order.emailDidYouMean,
    errorRequired: t.order.errorRequired,
    errorEmail: t.order.errorEmail,
    errorPhone: t.order.errorPhone,
    deliveryEU: t.order.deliveryEU,
    deliveryUS: t.order.deliveryUS,
    deliveryOther: t.order.deliveryOther,
    shippingFreeLabel: t.order.shippingValue,
    processing: t.order.processing,
    promoApply: t.order.promoApply,
    promoRemove: t.order.promoRemove,
    promoLoading: t.order.promoLoading,
    promoInvalid: t.order.promoInvalid,
    promoExpired: t.order.promoExpired,
    promoLimitReached: t.order.promoLimitReached,
    promoApplied: t.order.promoApplied,
    referralApplied: t.order.referralApplied,
    promoReplaceHint: t.order.promoReplaceHint,
    uaOrderBtn: t.order.uaOrderBtn || "Place order",
    uaDeliveryEstimate: t.order.uaDeliveryEstimate || "Nova Poshta delivery: <strong>1–3 business days</strong>",
    uaPhoneError: t.order.uaPhoneError || "Invalid phone number",
    uaPhonePlaceholder: t.order.uaPhonePlaceholder || "(0XX) XXX XX XX",
    npLoading: t.order.npLoading || "Loading...",
    npWarehouse: t.order.npWarehouse || "Nova Poshta department",
    npNotFound: t.order.npNotFound || "No departments found",
    shippingFreeUA: t.order.shippingFreeUA || "Free in Ukraine"
  }) %>;
  </script>
  <script src="/analytics.js"></script>
  <script src="/shared.js?v=3"></script>
  <% if (typeof extraJs !== 'undefined') { extraJs.forEach(function(js) { %>
  <script src="/<%= js %>?v=16"></script>
  <% }); } %>
  <script>
  /* Flag emoji polyfill for Windows (flags render as two-letter codes) */
  (function () {
    var isWindows = /Win/.test(navigator.platform || '');
    if (!isWindows) return;

    var sel = document.getElementById('country');
    if (sel) {
      for (var i = 0; i < sel.options.length; i++) {
        var v = sel.options[i].value;
        if (v && v.length === 2) {
          var txt = sel.options[i].textContent.replace(/(?:[\uD83C][\uDDE6-\uDDFF]){2}\s*/g, '').trim();
          sel.options[i].textContent = v + ' \u2014 ' + txt;
        }
      }
    }

    var phonePrefix = document.getElementById('phonePrefix');
    if (phonePrefix && sel) {
      var codes = {AT:'+43',BE:'+32',BG:'+359',HR:'+385',CY:'+357',CZ:'+420',DK:'+45',EE:'+372',FI:'+358',FR:'+33',DE:'+49',GR:'+30',HU:'+36',IE:'+353',IT:'+39',LV:'+371',LT:'+370',LU:'+352',MT:'+356',NL:'+31',PL:'+48',PT:'+351',RO:'+40',SK:'+421',SI:'+386',ES:'+34',SE:'+46',GB:'+44',US:'+1',CA:'+1',AU:'+61',JP:'+81',CH:'+41',NO:'+47',UA:'+380'};
      sel.addEventListener('change', function () {
        var c = this.value;
        if (c && c.length === 2) {
          phonePrefix.textContent = c + ' ' + (codes[c] || '+');
        }
      });
    }
  })();
  </script>
</body>
</html>
